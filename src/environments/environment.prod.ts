// ==========================================
// ConnectSphere — Production Environment
// ==========================================
// Used during production builds (ng build --configuration=production).
// Production should use same-origin paths and hosting-platform rewrites
// instead of a hardcoded localhost gateway URL.
// ==========================================

export const environment = {
  production: true,
  oauthBaseUrl: ''
};
